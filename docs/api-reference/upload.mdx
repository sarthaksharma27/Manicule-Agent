## Upload File
The current endpoint is `/upload`.
<ParamField query="file" type="file">The file to upload</ParamField>

## Health Check
<Warning>This endpoint is deprecated. Use `/health/v2` instead.</Warning>

The previous health check endpoint was `/health`. It has been replaced with a new version.

## Health Check v2
The current endpoint is `/health/v2`.